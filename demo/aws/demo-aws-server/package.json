{
  "name": "@perfect-stack/demo-aws-server",
  "version": "1.0.10",
  "description": "Demo of server on AWS",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@perfect-stack/nestjs-server": "^0.0.20"
  },
  "devDependencies": {
    "@types/node": "^17.0.9",
    "rimraf": "^3.0.2",
    "source-map-support": "^0.5.21",
    "typescript": "^4.5.4"
  }
}
